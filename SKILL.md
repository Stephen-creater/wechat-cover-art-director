---
name: wechat-cover-art-director
description: "为中文微信公众号文章策划并生成多套风格不同的 16:9 封面，并按用户的采用/淘汰反馈维护个人封面案例库。用于用户给出公众号标题或正文要封面、头图、题图，或对已生成的封面候选给出采用、淘汰、打分等反馈时。不用于 ds 微信发布流程内的封面、归藏风格卡片、小红书 3:4 图文或 PPT。"
---

# 微信公众号封面艺术指导

目标是像艺术指导一样先找到文章最值得视觉化的矛盾或隐喻，再生成多套真正不同的封面。吸引力来自概念、构图、配色和字体，不来自堆砌元素。

本 Skill 独立工作，不读取或依赖其他个人封面 Skill 或历史 Skill 目录。已被用户明确采用的图片、提示词和审美反馈保存在本 Skill 自己的资产库中。

每次开始前按顺序执行渐进式路由：

1. 阅读 [references/preferences.md](references/preferences.md) 掌握当前个人偏好。
2. 阅读 [references/router.md](references/router.md)，先按标题长度、排版容量、明暗、媒介和构图骨架选择模板；内容题材只用于替换隐喻，不再决定模板能否复用。
3. 只打开路由命中的 1-2 个 [references/cases/](references/cases/) 案例，不要一次加载全部案例。
4. 生成前阅读 [references/rejections.md](references/rejections.md)；需要查询具体淘汰提示词或反例时再读 [references/rejected-index.md](references/rejected-index.md)。
5. 批量探索新模板时读取 [references/template-space.md](references/template-space.md) 和 [references/creative-system.md](references/creative-system.md)，只从尚未覆盖的空间写提示词。
6. 生成前对新 prompt 做双向去重：既不能重跑已通过模板，也不能换色复刻已淘汰方向。
7. 写 prompt 时套用 [references/prompt-builder.md](references/prompt-builder.md) 骨架。
8. 生成后使用 [references/quality-gate.md](references/quality-gate.md) 验收，并再次排除已知失败模式。

## 输入

- 优先从用户消息或附件中识别文章标题和正文。
- 标题明确时保持原文、大小写、数字和标点，不擅自改写。
- 标题不明确时只追问标题，不先生成。
- 用户指定数量时照做；没有指定时默认生成 5 个方向。
- 标题和正文足够时直接执行，不要求用户预选风格。

## 创意分流

先提炼四项：

1. 文章的核心判断是什么。
2. 最有张力的对比或因果是什么。
3. 哪个具体物体、空间或行为能承载它。
4. 读者应该先感到好奇、震撼、清晰、温暖还是可信。

然后建立方向矩阵。每个方向至少在以下四项中的三项上不同：

- 背景与配色
- 版式与标题位置
- 视觉媒介
- 核心隐喻

同一批封面不能只是同一个模板换颜色，也不能全部是蓝紫科技风。

## 参考案例使用比例

- 前期案例库较小时，以原创为主：默认一批方向中最多 1 张直接适配最相近案例，1 张借用其构图或调色，其余方向原创。
- 匹配强度不足时不要强行套案例，直接原创。
- 每张生成图最多使用 2 个已通过案例：一个控制结构，一个控制材质或配色。
- 参考图片只从 `assets/approved-covers/` 选择。被拒绝的图和未明确采用的图不得进入正向参考。
- 用户以“探索模板”为目标时，题材相关性降为次要条件。允许把 FDE、RAG、信息爆炸、训练等旧标题用于新模板压力测试；优先覆盖短、中、长三种标题容量。
- 已经验证成功的模板不为增加样本数而重复生成。需要验证复用性时，只允许更换标题后做一次直接适配；发散批次默认继续探索新结构。

## 正向证据权重

案例进入正向库后仍需按用户反馈排序，不能同权处理：

- `强正向`：用户实际发布、实际采纳，或明确评价为同批最佳。可作为直接适配模板与主要参考图。
- `正常通过`：用户认为达到标准或“还可以”。可用于结构适配，但优先级低于强正向。
- `弱正向`：用户明确说“勉强过关、不是很满意、勉强够用”。只允许借用一个维度，不作为默认直接适配模板。

同一批得到明确名次时，把顺序写入案例、偏好和路由。后续选择模板时，先比较证据权重，再比较题材或构图相似度。

## 默认审美边界

- 用户不喜欢黑底。默认使用亮色、浅色、中性色或有空气感的彩色背景；纯黑或大面积近黑禁止作为默认方向。
- 一批 5 张中最多允许 1 张深色方向，而且必须由文章内容证明其必要性；用户明确不要深色时一张也不做。
- 封面需要吸睛，但避免花里胡哨。优先一个强主体、清晰轮廓、漂亮留白和有设计感的标题。
- 排版必须像完成度高的公众号封面，不像默认模板、PPT、教学课件或随手生成的信息图。
- 元素必须能解释文章内容。无法说明作用的图标、方块、线路、粒子、箭头和装饰全部删除。
- 默认不使用黑板、粉笔、课堂、卡通、黏土玩具、廉价赛博面板、通用机器人或发光大脑。
- 不把“科技文章”自动翻译成黑底、蓝紫霓虹、玻璃面板和缓存方块。

## 可选视觉家族

根据文章挑选，而不是每次全部使用：

- 明亮编辑杂志：浅米、纸白或柔和纯色背景，强标题与一个编辑摄影主体。
- 概念静物：用真实物体或高质感 3D 静物表达抽象机制。
- 超现实隐喻：熟悉物体发生一个不可能但容易理解的变化。
- 现代字体海报：字体和几何关系共同构成主视觉，不做廉价大字报。
- 精炼图解：只保留一个关系图，强调结构美，不做教学画板。
- 空间建筑：用路径、房间、层级、传送带或城市结构讲系统关系。
- 轻量拼贴：控制在 3-5 个素材层，保留杂志感和留白。

## 生成

- 使用当前环境的图片生成工具（Codex 中为内置 `image_gen`），每个方向独立调用并使用独立 prompt。
- 默认生成 16:9 PNG 横版封面。用户要求头条比例、方图或其他比例时再调整。
- 短标题一般为 1-3 行。长标题先拆成 `小引题 / 大主判断 / 小解释层` 三个语义层，只放大核心判断；解释层允许再换一行，总物理行数可到 4 行。不要把长标题压成三行同字号文字墙，也不要为了守行数把整段字缩得一样小。
- 默认只呈现用户标题，不添加副标题、栏目名、英文翻译、作者名或标签。
- 除标题外不允许出现模型擅自生成的可读文字、Logo、水印或二维码。
- 每个 prompt 都要写明具体构图、颜色、主体、标题区和明确禁用项，避免只堆风格形容词。

## 交付

- 候选保存到 `outputs/YYYY-MM-DD-<slug>/`：图片命名 `NN-E01.png`（探索）/ `NN-R01.png`（复用）；同目录写 `prompts.json`（每张最终 prompt）、`manifest.json`（编号、名称、分组、参考案例、prompt）、`README.md`（标题、方向表、质量门结果）和一张拼版预览 `preview.jpg`。
- 过程稿、检查脚本和断点记录放 `work/YYYY-MM-DD-<slug>/`（已被 git 忽略）；长批次在 `checkpoint.md` 记录进度，中断后从这里续跑。
- 用户筛选前，候选批次只留本地，不提交、不推送。
- 展示所有方向，并用一句话说明核心隐喻和视觉语言。
- 不替用户选最佳方案，除非用户主动询问。
- 用户反馈不喜欢时，优先识别审美原因并调整创意系统，不继续在失败模板上小修小补。
- 只有用户明确说“采用、通过、实际使用、收录”时，才把新图加入正向资产库。加入时同步更新图片、最终提示词、案例档案、索引标签和路由。
- 用户明确淘汰的结果只记录失败原因，不保存为正向案例。

## 异常与降级

- 当前环境没有图片生成工具时，不假装已生成：交付完整 `prompts.json` 与方向说明，并明确告知未出图。
- 单张标题文字出错（错字、多字、标点变形、额外可读文字）时，针对该问题重生成，最多 2 次；仍不达标就照常交付，并在 README 中逐张写明残留问题，由用户决定。不做像素层贴字修补。
- 标题缺失时只追问标题；正文缺失时可仅凭标题生成，但说明隐喻只基于标题。
- 路由没有合适案例时直接原创，不硬套。

## 已验证的易错点

- 全角标点常被渲染成半角，例如 `（Ontology）` 变 `(Ontology)`；验收时逐字核对括号、引号和冒号。
- 量具、仪表、书页、活字等道具容易长出可读数字或文字；prompt 里明确要求其表面无刻度、无字，并在验收时专门检查。
- 长标题容易被压成等字号文字墙，或在语义短语中间断行；先按语义拆层，再写断行。
- “科技文章”容易被默认翻译成黑底蓝紫霓虹；生成前核对整批明暗分布。

## 资产闭环

新增一个通过案例时，完成以下五项：

1. 图片复制到 `assets/approved-covers/Axx-<slug>.png`。
2. 最终可复现提示词写入 `references/cases/Axx-<slug>.md`。
3. 在 [references/approved-index.md](references/approved-index.md) 添加题材、视觉、配色和构图标签。
4. 在 [references/router.md](references/router.md) 增加或强化对应路由。
5. 若用户同时说明喜欢或不喜欢的原因，更新偏好或拒绝模式；整轮反馈较多时另写 `references/feedback/roundNN-<slug>.md`，并在偏好中链接。

新增淘汰案例时：图片可获得则复制到 `assets/rejected-covers/<batch>/`，最终提示词复制到 `references/rejected-prompts/<batch>/`，并更新负向索引。批次淘汰但缺少逐张原因时，标注证据强度，不擅自编造具体失败原因。

## GitHub 同步纪律

本 Skill 的公开远程仓库是 `https://github.com/Stephen-creater/wechat-cover-art-director`，默认分支是 `main`。

任何对本 Skill 文件夹的修改，包括图片、案例、提示词、偏好、路由、正负索引和入口规则，都必须在同一任务闭环中完成：

1. 验证文件、数量、引用和敏感信息。
2. 检查 `git status` 与暂存差异。
3. 创建有意义的 Git commit。
4. 推送到 `origin/main`。
5. 从 GitHub 读回仓库可见性、远程提交和关键文件，确认远程状态真实更新。

存在未提交或未推送的 Skill 变更时，不得向用户声称 Skill 更新完成。除非用户明确要求只做本地草稿，否则不省略 commit 或 push。
